paths:
  .github/workflows/deploy.yml:
    # GitHub added concurrency.queue in May 2026; actionlint 1.7.12 has not added it yet.
    ignore:
      - 'unexpected key "queue" for "concurrency" section'
